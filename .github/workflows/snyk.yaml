name: Snyk

on:
  release:
    types: [published]
  pull_request:

jobs:
  monitor:
    uses: ocadotechnology/codeforlife-workspace/.github/workflows/monitor-python-release.yaml@main
    secrets: inherit
    with:
      python-version: 3.12