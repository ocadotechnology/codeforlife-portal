version: '3.4'

services:
  codeforlifeportal:
    image: codeforlifeportal
    build:
      context: .
      dockerfile: ./Dockerfile
    command: ./run 0.0.0.0:8000
    volumes: 
        - .:/app/
    ports:
      - 8000:8000
